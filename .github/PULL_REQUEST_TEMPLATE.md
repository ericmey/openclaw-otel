### Summary
<!-- One or two sentences on what this PR changes and why. -->

### Field-shape impact
<!-- Does this change what fields the plugin emits? If yes:
       - which signals (logs / metrics / traces) are affected
       - which labels / attributes / metric names change
       - whether downstream queries / dashboards need updates
     If no, write "no field-shape changes". -->

### Tests
- [ ] `npm run typecheck` passes
- [ ] `npm test` passes
- [ ] New behavior is covered by a test

### Related
<!-- Issues this closes or related discussion. Audit-finding IDs (Fx.y) when applicable. -->
